# Minimal dependency set for running the test suite in CI.
# Excludes openai-whisper (and its heavy torch stack): Whisper is imported
# lazily at runtime and no test exercises transcription, so it isn't needed
# to validate the codebase. Full app deps live in requirements.txt.
fastapi==0.115.0
spotipy==2.24.0
Pillow==10.4.0
mutagen==1.47.0
requests==2.32.3
python-dotenv==1.0.1
pydantic-settings==2.5.0
pytest==8.3.0
